import styled from "styled-components";

const CenteredLayout = styled.main`
	display: grid;
	place-items: center;
	position: absolute;
	top: 0;
	left: 0;
	height: 100%;
	width: 100%;
`;

export default CenteredLayout;